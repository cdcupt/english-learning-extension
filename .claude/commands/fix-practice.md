The user has found a bug in one of the practice modules (Reading, Writing, Vocabulary, Speaking, or Listening). Ask which module and what the issue is, then:

1. Read the relevant page component in `src/newtab/pages/`
2. Read related shared code (`shared/api/claude.ts`, `shared/storage.ts`, `shared/types.ts`) as needed
3. Fix the issue
4. Run `npm run build` to verify
5. Tell the user to reload the extension
